name: Unit test
on: push

jobs:
  test_project:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run the Calculator Unit Tests
      run: |
      python -m pip install --upgrade pip
      pip install flake8 unittest